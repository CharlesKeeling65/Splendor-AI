# 网页规则实测记录（E1–E6）与性能留档

> 对应 [plan/phase-0-foundation.md](../plan/phase-0-foundation.md) §2 T0.4 / §5 M0.1-M0.2。
> 实测对象：https://game.hullqin.cn/ccbs（HullQin《璀璨宝石》网页版）。
> **礼仪约束**：只在自建房间操作；点击频率保持人类量级；不进入真人房间。
> 状态标记：✅ 已实测并有结论 / 🔶 部分实测（含推断）/ ⬜ 待实测。

---

## M0.2 掩码构建吞吐对比（2026-09-05，本机 Apple Silicon / Python 3.13）

对比对象：`create_legal_actions_mask` + `create_action_mapping` 改造前（`_slow_*`，O(3510) 线性扫描）
与改造后（ACTION_INDEX 缓存，O(1) 查表）。样本状态含 24 个合法动作，各重复 20 次取均值：

| 实现 | 掩码构建 | 映射构建 | 合计/步 |
|---|---|---|---|
| 旧（线性扫描） | 2.298 ms | 2.272 ms | 4.570 ms |
| 新（缓存查表） | 0.032 ms | 0.029 ms | 0.061 ms |
| **提速** | ×72.0 | ×78.9 | **×75.3** |

结论：达到"≥50× 掩码构建提速"的验收线。训练路径每步两次调用（step 内 mapping + mask），
1×10⁶ 步训练可节省约 75 分钟纯索引开销（单核估算）。

---

## E1 多贵族选择 UI（决定 action_executor 贵族分支）

- **判定问题**：一回合同时满足多个贵族时，网页如何呈现选择？能否不选？
- **操作序列**：构造满足 ≥2 个贵族条件的局面（自建 2 人房，对面脚本/手动配合）。
- **DOM 前后快照**：（待实测补充）
- **结论**：⬜ 待实测（引擎侧语义：`getLegalActions` 为每个可 visit 贵族生成独立动作，`noble_index` 维度必选其一）。

## E2 >10 宝石返还子流程（决定执行器返还分支）

- **判定问题**：取宝石导致手宝石 >10 时，网页的返还 UI 流程与点击序列？能否返还刚拿的颜色？
- **操作序列**：囤 9 颗宝石后取 2 同色（引擎侧会生成 `returned_gems` 非空的变体动作，且引擎禁止返还刚拿的颜色）。
- **DOM 前后快照**：（待实测补充）
- **结论**：⬜ 待实测。

## E3 终局结算 DOM（决定 browser_env 终局检测）

- **判定问题**：终局页面的 DOM 特征（结束信号选择器、比分展示、重开按钮）。
- **操作序列**：打到 ≥15 分触发终局。
- **DOM 前后快照**：（待实测补充）
- **结论**：⬜ 待实测（候选信号见 BROWSER_RL_MAPPING §5.3）。

## E4 空牌库 reserve 是否发金（执行器 + 引擎一致性）

- **判定问题**：牌库为空时预定桌面卡是否仍 +1 金？
- **操作序列**：清空某 tier 牌库后预定该 tier 桌面卡。
- **结论**：⬜ 待实测（引擎语义：`collected_gems = {"yellow":1} if board.gems["yellow"]>0 else {}`，与牌库余量无关——若网页不同则记差异）。

## E5 自愿少拿宝石（全计划最高风险项：R2 规则奇偶）

- **判定问题**：手宝石 ≤7 时，网页能否只拿 1~2 颗（标准规则）？
  引擎是**非标准规则**：强制最少拿 `min(3, 可用色数)` 个（`splendor_model.py:443-452`）。
- **影响**：若网页允许少拿 → 两侧合法动作集不一致，网页上会出现本地掩码之外的动作；
  需裁决"给引擎加 `standard_take_rules` 开关"还是"接受差异"（ADR）。
- **结论**：⬜ 待实测。

## E6 同色 7 张购卡上限（规则奇偶）

- **判定问题**：网页是否存在"同色已购 7 张禁买"的限制？
- **结论**：⬜ 待实测。

---

## 实测会话纪律（每次实测必读）

1. 自建房间（2 人），第二座位由本人另一身份或挂机座席填充；
2. 每次点击间隔 ≥0.5s，整局时长与人类对局同量级；
3. 实测结束正常离开房间，不留挂机连接。
