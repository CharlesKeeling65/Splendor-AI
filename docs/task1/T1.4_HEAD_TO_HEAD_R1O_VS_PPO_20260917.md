# T1.4 unbound head-to-head observation（quarantined）

日期：2026-09-17

状态：**不作为实验结果或决策证据**。

## 保留原因

一次交互式、只读诊断曾记录：在 non-sealed validation-A 的 200 个开局上，
`r1-O/update-1850` 与 frozen C4-R2 `ppo-best` 交换双座次共 400 局，内存汇总为
`201-0-199`；排除与 10-row selector bank 重叠的开局后为 `191-0-189`。

该观察保留在这里是为了避免把已经看过的数字遗忘后重新包装成“未观察”的假设，
而不是为了认可它。仓库和 retry-4 completion receipt 中均找不到对应的 episode ledger、
evaluation manifest、schedule hash 或 content-addressed statistics artifact；原运行只留下了
手工汇总文字。因此无法复核 400 局分母、逐局动作、失败处理、座次映射或代码 provenance。

## 明确排除

- [`T1.4_PILOT_ANALYSIS_20260919.ipynb`](T1.4_PILOT_ANALYSIS_20260919.ipynb)
  没有读取或使用上述数字；
- 它不能证明 `r1-O` 与 `ppo-best` 同强或谁更强；
- 它不能用于 deployment scenario power、final N、checkpoint 排名、validation-B 或
  sealed-test 决策；
- `r1-O` 的 45/60 仍只是 best-of-41 selector 结果，不因这条观察而升级为独立验证。

## 若要恢复该问题

必须用新的 joint、manifest-bound、non-sealed paired evaluation 重跑，并持久化完整
episode ledger、candidate/opponent/checkpoint/code/bank/schedule hashes 与正式
`statistics.json`。在该 artifact 获批并完成前，本文件只是一条 quarantined observation。
